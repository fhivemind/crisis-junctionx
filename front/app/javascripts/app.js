// Import the CSS.
import '../stylesheets/app.css'
require('log-timestamp');


/// Defining app.
window.App = {
    start: async function() {
            /* -------------------------------------------------------------- 
             *                  CONTROLLERS
             * -------------------------------------------------------------- */

            /**
             * Listing controller.
             */
            if ($("#post-controller").length > 0) {
            }
    }
}